  <%- @items.each do |item| -%>
  <div class="text-center">
      <%= link_to image_tag(item.image),
          item_path(item)%>
      <p><%= link_to item.name.capitalize, item_path(item) %></p>
      <hr>
  </div>

  <%- end -%>

<%= paginate @items %>
