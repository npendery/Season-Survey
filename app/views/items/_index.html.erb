  <%- @items.each do |item| -%>
  <div class="text-center">
      <%= link_to image_tag(item.image, size: "300x300"),
          item_path(item) %>
      <h3><%= link_to item.name.capitalize, item_path(item) %></h3>
      <hr>
  </div>

  <%- end -%>

<%= paginate @items %>
