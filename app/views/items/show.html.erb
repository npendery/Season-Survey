<div class="row">
  <div class="columns small-4">
    <div class="float_text"><%= @item.name %> </div>
      <br>
    <div class="right_image"><img src=<%= @item.image %>></div>
    <br>
    <% if @item.description.present? %>
      <h3> Description </h3>
        <div>
          <%= @item.description %>
        </div>
    <% end %>

    <% if @item.reviews.present? %>
      <h3> Reviews </h3>
        <div>
          <% @item.reviews.each do |review| %>
            <p><%= review.rating %></p>
            <p><%= review.description %></p>

            <% if @user == review.user %>
              <p><%= link_to "Edit Review", edit_item_review_path(@item, review) %></p>
            <% end %>

          <% end %>
        </div>
    <% end %>

    <%= link_to "Add Review", new_item_review_path(@item) %>

<% if @item.user == current_user %>
  <div><%= link_to "Update Item", edit_item_path(@item) %></div>
  <br>
  <br>
  <div><%= link_to "Delete Item", item_path(@item), method: :delete %></div>
<% end %>
<br>
<br>
<%= link_to "Back", root_path %>
